# check if file exists
if [[ -f "$PROJECTR_COMMANDS_FOLDER/tools/check_node_modules" ]]
then
    "$PROJECTR_COMMANDS_FOLDER/tools/check_node_modules"
elif [[ -f "$PWD/settings/commands/tools/check_node_modules" ]]
then
    "$PWD/settings/commands/tools/check_node_modules"
fi